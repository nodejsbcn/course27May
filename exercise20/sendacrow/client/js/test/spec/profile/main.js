/*describe('ProfileController', function() {
	beforeEach(module('AqualogyApp'));

	var ctrl, scope;

	beforeEach(inject(function ($controller, $rootScope) {
		scope = $rootScope.$new();		

		createController = function() {
			return $controller('ProfileController', {
				'$scope': scope
			});
		};
	}));	

	it('should create $scope.sup when calling saveProfile', function() {
		var controller = createController();
		expect(scope.sup).toBeUndefined();
		scope.saveProfile();
		expect(scope.sup).toEqual('Suuup!');
	});
});*/